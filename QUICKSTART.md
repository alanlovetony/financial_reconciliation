# 快速开始指南 🚀

## 5分钟上手财务对账工具

### 第一步: 环境准备

1. **确保已安装 Python 3.8+**
   ```bash
   python --version
   ```

2. **安装依赖包**
   ```bash
   pip install -r requirements.txt
   ```

### 第二步: 启动应用

```bash
streamlit run financial_reconciliation.py
```

启动后会自动打开浏览器，或手动访问: http://localhost:8501

### 第三步: 准备数据文件

你只需要准备 **2个文件**（CSV 或 Excel 格式）：

#### 文件1: 有益云数据
直接从有益云系统导出的数据文件。系统会**自动识别**：
- 带 `【GFYH】` 的记录 → **T+N**（广发银行通道）
- 不带 `【GFYH】` 的记录 → **T+1**（财付通通道）

#### 文件2: 银行流水
必须包含以下列：
- `发生时间` - 银行入账时间
- `收入` - 入账金额
- `用途` - 交易用途
- `附言` - 附言信息

### 第四步: 上传文件并对账

1. 在界面上分别上传2个文件
2. 查看数据预览，确认格式正确
3. 系统会显示 T+1 和 T+N 的记录数量
4. 点击"开始对账"按钮
5. 等待处理完成（通常几秒钟）
6. 查看对账结果

### 第五步: 查看结果

对账报告会显示：
- 🟢 **绿色行**: 匹配成功的记录
- 🟡 **黄色行**: 银行多收的记录
- 🔴 **红色行**: 未匹配或银行少收的记录

关键指标：
- **匹配成功数**: 有多少天的业务成功匹配到银行流水
- **未匹配数**: 有多少天的业务未找到对应银行流水
- **系统应收**: T+1 + T+N 的总金额
- **银行实收**: 银行流水的总收入
- **差异金额**: 系统应收与银行实收的差异

### 第六步: 导出报告

点击"下载完整Excel报告"按钮，报告包含：
- 对账汇总
- 项目明细透视表
- 银行流水明细
- T+1 原始数据
- T+N 原始数据

## T+1 与 T+N 识别规则

系统通过 `捐赠说明` 字段自动识别：

| 特征 | 分类 | 收款通道 | 到账规则 |
|------|------|---------|---------|
| 包含 `【GFYH】` | T+N | 广发银行广发易汇 | 银行附言中的日期 = 业务日期 |
| 不包含 `【GFYH】` | T+1 | 财付通（微信支付） | 到账日期 - 1天 = 业务日期 |

## 常见使用场景

### 场景1: 日常对账
每天下载有益云数据和银行流水，上传到工具进行对账，快速发现未到账项目。

### 场景2: 月度核对
月末汇总整月数据，进行全面对账，生成月度对账报告。

### 场景3: 问题排查
当发现金额不符时，通过详细报告中的"银行摘要"和"匹配来源"字段快速定位问题。

## 提示与技巧

### 💡 数据准备
1. **有益云数据**: 直接导出即可，无需任何预处理
2. **银行流水**: 确保包含发生时间、收入、用途、附言列

### 💡 处理未匹配项
1. **检查时间**: 可能是银行还未到账
2. **核对金额**: 检查是否有手续费或其他扣款
3. **查看备注**: 通过银行摘要信息人工核对

### 💡 批量处理
如果有多个月的数据需要对账：
1. 可以将所有有益云数据合并到一个文件
2. 将所有银行流水合并到一个文件
3. 一次性完成对账

## 故障排除

### 问题1: 文件上传失败
- 检查文件格式是否为 CSV 或 Excel
- 检查文件是否损坏
- 尝试用 Excel 重新保存文件

### 问题2: 列名错误
- 有益云数据: 确保有 `捐赠项目`、`捐赠时间`、`捐赠金额`、`捐赠说明` 列
- 银行流水: 确保有 `发生时间`、`收入`、`用途`、`附言` 列

### 问题3: 日期解析失败
- 检查日期格式是否正确
- 常见格式: 2024-01-01, 2024/01/01, 2024-01-01 10:30:00
- 避免使用纯文本格式的日期

### 问题4: 匹配率低
- 检查金额是否完全一致（包括小数位）
- 确认银行到账日期是否在业务日期之后
- 查看银行备注是否包含日期信息

## 下一步

- 阅读 [README.md](README.md) 了解详细功能
- 查看 [sample_data_format.md](sample_data_format.md) 了解数据格式示例

## 需要帮助？

如遇到问题，请检查：
1. Python 版本是否 >= 3.8
2. 所有依赖包是否正确安装
3. 数据文件格式是否符合要求
4. 列名是否完全匹配

祝使用愉快！ 💰✨
