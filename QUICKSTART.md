# 快速开始指南 🚀

## 5分钟上手财务对账工具

### 第一步: 环境准备

1. **确保已安装 Python 3.8+**
   ```bash
   python --version
   ```

2. **安装依赖包**
   ```bash
   pip install -r requirements.txt
   ```

### 第二步: 启动应用

```bash
streamlit run financial_reconciliation_simple.py
```

启动后会自动打开浏览器，或手动访问: http://localhost:8501

### 第三步: 准备数据文件

你只需要准备 **2个文件**（CSV 或 Excel 格式）：

#### 文件1: 有益云数据
直接从有益云系统导出的数据文件。系统会**自动识别**：
- 带 `GFYH` 的记录 → **T+N**（广发银行通道）
- 不带 `GFYH` 的记录 → **T+1**（财付通通道）

#### 文件2: 银行流水
必须包含以下列：
- `发生时间` - 银行入账时间
- `收入` - 入账金额
- `用途` - 交易用途
- `附言` - 附言信息

### 第四步: 上传文件并对账

1. 在界面上分别上传2个文件
2. 查看数据预览，确认格式正确
3. 系统会显示 T+1 和 T+N 的记录数量
4. 点击"开始对账"按钮
5. 等待处理完成（通常几秒钟）
6. 查看对账结果

### 第五步: 查看结果

对账结果以**逐笔核对**的方式展示：

| 序号 | 到账日期 | 银行金额 | 匹配类型 | 交易笔数 | 匹配业务日期 | 系统金额 | 状态 |
|-----|---------|---------|---------|---------|-------------|---------|------|
| 1 | 12/02 | ¥1,962.88 | T+1 | 11 | 12/01 | ¥1,962.88 | ✅ 匹配 |
| 2 | 12/02 | ¥2,204.68 | - | 0 | - | ¥0 | ❌ 未匹配 |

四个Tab页面：
- 🟢 **逐笔核对**: 每笔银行流水的匹配结果
- 🔴 **未匹配**: 银行有流水但系统没有对应业务
- 🟡 **未到账**: 系统有业务但银行没收到钱
- 📊 **项目明细**: 每个项目每天的捐赠金额

### 第六步: 导出报告

点击"下载完整对账报告"按钮，报告包含：
- 银行流水逐笔对账
- 未匹配银行流水
- 未到账业务
- 项目明细透视表
- 有益云原始数据
- 银行流水原始数据

## 🎯 对账规则说明

### 匹配逻辑

**以银行到账日期为主线，逐笔核对：**

1. **先匹配 T+1**（不带GFYH的业务）
   - 银行到账日期 - 1天 = 业务日期
   - 例：12/2到账 → 匹配12/1的T+1业务汇总

2. **再匹配 T+N**（带GFYH的业务）
   - 优先从银行附言/用途中提取业务日期
   - 如：附言 `1201_xxx` → 匹配12/01的业务
   - 若无法提取，则倒查过去30天

3. **匹配条件**
   - 金额必须**完全相等**才算匹配成功

### 字段说明

| 字段 | 说明 |
|-----|------|
| 匹配类型 | T+1 或 T+N（显示实际天数如 T+2, T+3） |
| 交易笔数 | 该业务日期在有益云系统中的捐赠笔数 |
| 匹配业务日期 | 银行流水对应的有益云业务日期 |
| 系统金额 | 有益云系统中该日期的汇总金额 |

## 常见使用场景

### 场景1: 日常对账
每天下载有益云数据和银行流水，上传到工具进行对账，快速发现未到账业务。

### 场景2: 月度核对
月末汇总整月数据，进行全面对账，生成月度对账报告。

### 场景3: 问题排查
当发现有未匹配的银行流水时，通过银行备注信息手工核对来源。

## 💡 提示与技巧

### 数据准备
1. **有益云数据**: 直接导出即可，无需任何预处理
2. **银行流水**: 确保包含发生时间、收入、用途、附言列

### 处理未匹配项
1. **银行流水未匹配**: 可能是其他渠道的收款，非有益云业务
2. **业务未到账**: 可能银行还未处理，或需要核对金额

### 批量处理
如果有多个月的数据需要对账：
1. 可以将所有有益云数据合并到一个文件
2. 将所有银行流水合并到一个文件
3. 一次性完成对账

## 🔧 故障排除

### 问题1: 文件上传失败
- 检查文件格式是否为 CSV 或 Excel
- 检查文件是否损坏
- 尝试用 Excel 重新保存文件

### 问题2: 列名错误
- 有益云数据: 确保有 `捐赠项目`、`捐赠时间`、`捐赠金额`、`捐赠说明` 列
- 银行流水: 确保有 `发生时间`、`收入`、`用途`、`附言` 列

### 问题3: 匹配率低
- 金额必须完全一致（包括小数位）
- 确认银行附言格式是否正确（如 `1201_xxx`）
- 某些银行流水可能不是有益云业务

### 问题4: 页面显示异常
- 刷新页面重试
- 检查浏览器是否为最新版本
- 尝试使用 Chrome 或 Edge 浏览器

## 📚 下一步

- 阅读 [README.md](README.md) 了解详细功能
- 查看 [sample_data_format.md](sample_data_format.md) 了解数据格式示例

## 🆘 需要帮助？

如遇到问题，请检查：
1. Python 版本是否 >= 3.8
2. 所有依赖包是否正确安装
3. 数据文件格式是否符合要求
4. 列名是否完全匹配

祝使用愉快！ 💰✨
